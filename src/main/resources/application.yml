server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/golf_db
    username: myuser
    password: Keyin1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect